import { SET_IDENTITY_NUMBER } from './ActionTypes';
import { createAction } from 'redux-actions';

export const setIdentityAction = createAction(SET_IDENTITY_NUMBER);

// export const setIdentity = () => {
    
//     return 
  
// };